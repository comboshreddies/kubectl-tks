
TODO:
# DONE
#Add -T switch to terminate tmux if same session exists
#catch prompt as regexp

In k8s.go consider senting containers name list []string, currently not needed

# DONE - one-liners are supported now, no need for -C flag
#Add -C flag to run one liner without specifying script name

OP_RECORD (each pod window capture in file per pod)

Check timings check, make different ctrl+c or ctrl+\ (detach/terminate)

expand shortcuts in info
